echo "--- pre-push: running quality checks ---"

echo "[1/3] Prettier format check..."
npm run format:check || { echo "FAILED: Unformatted files found. Run 'npm run format' then commit before pushing."; exit 1; }

echo "[2/3] ESLint..."
npm run lint || { echo "FAILED: ESLint found errors. Fix them before pushing."; exit 1; }

echo "[3/3] TypeScript type check..."
npm run typecheck || { echo "FAILED: TypeScript errors found. Fix them before pushing."; exit 1; }

echo "--- All checks passed. Proceeding with push. ---"
